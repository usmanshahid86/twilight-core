//go:build linux

package types

// The omitted expression list repeats the preceding one, so ModuleName is
// "coreslot" here even though its ValueSpec carries no Values entry.
const (
	sender = "coreslot"
	ModuleName
)
