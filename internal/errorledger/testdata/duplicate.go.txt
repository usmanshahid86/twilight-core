package types

import errorsmod "cosmossdk.io/errors"

const ModuleName = "probe"

var (
	ErrOne = errorsmod.Register(ModuleName, 2, "one")
	ErrTwo = errorsmod.Register(ModuleName, 2, "duplicate code")
)
